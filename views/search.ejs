<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Titillium+Web:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container mt-5">
        <h1>Search Results for Cocktails</h1>
        <div class="row">
            <% if (cocktails && cocktails.length > 0) { %>
                <% cocktails.forEach(cocktail => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100">
                          
                            <a href="/cocktail/<%= cocktail.idDrink %>">
                                <img src="<%= cocktail.strDrinkThumb %>" class="card-img-top rounded" alt="<%= cocktail.strDrink %>">
                            </a>
                            <div class="card-body">
                                <h5 class="card-title">
                                    <a href="/cocktail/<%= cocktail.idDrink %>"><%= cocktail.strDrink %></a>
                                </h5>
                                <p class="card-text"><%= cocktail.strInstructions.substring(0, 100) %>...</p>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p>Sorry, no cocktails found! :c</p>
            <% } %>
        </div>
    </div>
</body>
</html>
